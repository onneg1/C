peça 5 números inteiros e identifique os pares e os impares contando.



#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main () {

    setlocale (LC_ALL, "portuguese");

    int i, n, par = 0, impar = 0

    for (i = 1; i <= 5; i++) {
        printf("Digite o %d número: ", i);
        scanf("%d", &n);
     if (n % 2 == 0) {
        par += 1;
     } else {
        impar += 1;}
 
     }
}
    printf ("Numeros impares: %d", impar);
    printf ("Numeros pares: %d", par);
    
    return 0;
}
